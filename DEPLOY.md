# คู่มือการ Deploy korkornor บน Vercel

## 🎭 สถานะปัจจุบัน: Mockup Mode

เว็บไซต์กำลังอยู่ในโหมดจำลอง (Mockup) - ข้อมูลที่แสดงเป็นข้อมูลตัวอย่าง
- ✅ แผนที่แสดงผลได้ (ข้อมูลจำลอง 25 หน่วย)
- ✅ สถิติแสดงผลได้
- ✅ ฟอร์มรายงานทำงานได้ (แต่ไม่บันทึกจริง)
- ⚠️ AI เป็นระบบจำลอง (ไม่ได้เชื่อม Gemini)
- ⚠️ ไฟล์รูป/วิดีโอไม่ถูกอัปโหลดจริง

---

## 🚀 วิธี Deploy ง่ายที่สุด (Connect GitHub)

### ขั้นตอนที่ 1: Push โค้ดขึ้น GitHub

```bash
# 1. สร้าง Git Repository ใหม่บน GitHub (ชื่อ korkornor)
# 2. แล้วรันคำสั่งนี้ในโฟลเดอร์โปรเจกต์

cd korkornor
git init
git add .
git commit -m "Initial commit - Mockup version"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/korkornor.git
git push -u origin main
```

### ขั้นตอนที่ 2: Connect กับ Vercel (ง่ายมาก!)

1. ไปที่ [vercel.com/new](https://vercel.com/new)
2. ลงชื่อเข้าใช้ด้วย GitHub
3. **Import Git Repository** → เลือก `korkornor`
4. Vercel จะตรวจจับอัตโนมัติว่าเป็น Next.js
5. กด **Deploy**

เสร็จ! 🎉 ไม่ต้องตั้งค่าอะไรเพิ่ม (ตอนนี้เป็น Mockup ไม่ต้องใส่ API Keys)

### ขั้นตอนที่ 3: ตั้งค่า Subdomain

1. ไปที่ [vercel.com/dashboard](https://vercel.com/dashboard)
2. เลือกโปรเจกต์ `korkornor`
3. ไปที่ **Settings** → **Domains**
4. ใส่ `korkornor` แล้วกด **Add**
5. เว็บจะอยู่ที่ `https://korkornor.vercel.app`

---

## 🔧 การอัปเดตเว็บไซต์ (หลังจาก Connect แล้ว)

ทุกครั้งที่ push โค้ดขึ้น GitHub, Vercel จะ deploy อัตโนมัติ:

```bash
git add .
git commit -m "แก้ไข UI"
git push
# Vercel จะ build และ deploy อัตโนมัติ!
```

---

## 🔌 ขั้นตอนการเชื่อมต่อ Supabase (เมื่อต้องการใช้งานจริง)

### 1. สร้าง Supabase Project

1. ไปที่ [supabase.com](https://supabase.com)
2. สร้างโปรเจกต์ใหม่
3. ไปที่ **SQL Editor**
4. คัดลอกโค้ดจาก `supabase/schema.sql` แล้วรัน

### 2. นำเข้าข้อมูลหน่วยเลือกตั้ง

**วิธีที่ 1:** รัน SQL โดยตรง
```sql
INSERT INTO election_units (province, district, sub_district, unit_number, latitude, longitude, voter_count)
VALUES 
  ('กรุงเทพมหานคร', 'เขตพญาไท', 'สามเสนใน', 1, 13.7563, 100.5018, 523),
  -- ... เพิ่มต่อ
```

**วิธีที่ 2:** ใช้ Script
```bash
# ตั้งค่า environment
export NEXT_PUBLIC_SUPABASE_URL="your-url"
export SUPABASE_SERVICE_ROLE_KEY="your-key"

# รัน script
node scripts/import-election-units.js data/election-units.json
```

### 3. ตั้งค่า Environment Variables บน Vercel

ไปที่ Vercel Dashboard → Project → Settings → Environment Variables:

| Variable | Value | หาได้จากไหน |
|----------|-------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://xxxx.supabase.co` | Supabase Settings → API |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJ...` | Supabase Settings → API (anon public) |
| `SUPABASE_SERVICE_ROLE_KEY` | `eyJ...` | Supabase Settings → API (service_role) |
| `GEMINI_API_KEY` | `AI...` | [Google AI Studio](https://makersuite.google.com) |
| `BLOB_READ_WRITE_TOKEN` | `vercel_blob...` | Vercel Dashboard → Storage → Blob |

### 4. แก้ไขโค้ดให้ใช้งานจริง

แก้ไข `src/lib/supabase.ts`:
- ลบ comment `/* ... */` ที่หุ้มโค้ดด้านล่าง
- หรือแก้ไข `IS_MOCK_MODE = false` ใน `src/lib/mockMode.ts`

### 5. Deploy ใหม่

```bash
git add .
git commit -m "Connect to Supabase"
git push
```

---

## 📋 Checklist ก่อนใช้งานจริง

- [ ] สร้าง Supabase project
- [ ] รัน schema.sql
- [ ] นำเข้าข้อมูลหน่วยเลือกตั้ง (95,000+ หน่วย)
- [ ] สร้าง Gemini API Key
- [ ] สร้าง Vercel Blob Store
- [ ] ใส่ Environment Variables ใน Vercel
- [ ] แก้ไขโค้ดให้ใช้งานจริง (ปิด Mock Mode)
- [ ] ทดสอบส่งรายงานจริง
- [ ] ตั้งค่า Custom Domain (ถ้ามี)

---

## 🆘 แก้ไขปัญหาเบื้องต้น

### แผนที่ไม่แสดง
- ตรวจสอบว่า Leaflet CSS ถูกโหลด
- ดู Console ว่ามี error อะไร

### ไม่สามารถส่งรายงานได้
- ตรวจสอบว่ายังอยู่ใน Mock Mode (ตอนนี้เป็น mock อยู่แล้ว ไม่มีปัญหา)
- ถ้าเชื่อม Supabase แล้วมีปัญหา → ดู Vercel Logs

### AI ไม่ทำงาน
- ตรวจสอบ Gemini API Key
- ตรวจสอบว่าเปิดใช้งาน API ใน Google Cloud หรือยัง

---

## 💡 Tips

1. **ใช้ Preview Deployments**: ทุกครั้งที่สร้าง Pull Request, Vercel จะสร้าง URL สำหรับ preview ให้ทดสอบก่อน merge

2. **Analytics**: เปิดใช้งาน Vercel Analytics ได้ฟรี เพื่อดูสถิติผู้ใช้งาน

3. **Speed Insights**: ตรวจสอบความเร็วเว็บไซต์แบบ real-time

---

#8กุมภาเห็นชอบ 🇹🇭 #รัฐธรรมนูญฉบับประชาชน
