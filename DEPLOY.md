# คู่มือการ Deploy korkornor บน Vercel

## 🎭 สถานะปัจจุบัน: Mockup Mode

เว็บไซต์กำลังอยู่ในโหมดจำลอง (Mockup) - ข้อมูลที่แสดงเป็นข้อมูลตัวอย่าง
- ✅ แผนที่แสดงผลได้ (ข้อมูลจำลอง 25 หน่วย)
- ✅ สถิติแสดงผลได้
- ✅ ฟอร์มรายงานทำงานได้ (แต่ไม่บันทึกจริง)
- ⚠️ AI เป็นระบบจำลอง (ไม่ได้เชื่อมต่อ OpenRouter)
- ⚠️ ไฟล์รูป/วิดีโอไม่ถูกอัปโหลดจริง

---

## 🔌 การเชื่อมต่อ OpenRouter (AI ฟรี!)

### 1. สมัคร OpenRouter
1. ไปที่ [openrouter.ai](https://openrouter.ai)
2. สมัครบัญชีฟรี
3. ไปที่ Settings → API Keys
4. สร้าง API Key ใหม่ (ขึ้นต้นด้วย `sk-or-v1-`)

### 2. Models ฟรีที่แนะนำ
| Model | ใช้สำหรับ | Context |
|-------|-----------|---------|
| `arcee-ai/trinity-large-preview` | จัดหมวดหมู่รายงาน | 131K |
| `tngtech/deepseek-tng-r1t2-chimera` | เปรียบเทียบรายงานซ้ำ | 164K |
| `z-ai/glm-4.5-air` | อ่านรูปป้ายหน่วยเลือกตั้ง | 131K |
| `stepfun/step-3.5-flash` | งานเร็ว | 256K |

**หมายเหตุ:** ทุก model ฟรี `$0/M input tokens`

### 3. ตั้งค่า Environment Variable

ไปที่ Vercel Dashboard → Project → Settings → Environment Variables:

```
OPENROUTER_API_KEY=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
```

### 4. Redeploy

```bash
cd korkornor
git push
# Vercel จะ deploy อัตโนมัติ
```

---

## 🚀 วิธี Deploy ง่ายที่สุด (Connect GitHub)

### ขั้นตอนที่ 1: Push โค้ดขึ้น GitHub

```bash
cd korkornor
git init
git add .
git commit -m "Initial commit - Mockup version"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/korkornor.git
git push -u origin main
```

### ขั้นตอนที่ 2: Connect Vercel

1. ไปที่ [vercel.com/new](https://vercel.com/new)
2. ลงชื่อเข้าใช้ด้วย GitHub
3. **Import** โปรเจกต์ `korkornor`
4. Vercel จะตรวจจับอัตโนมัติว่าเป็น Next.js
5. ไม่ต้องใส่ Environment Variables (ตอนนี้เป็น Mockup)
6. กด **Deploy**

### ขั้นตอนที่ 3: ตั้งค่า Subdomain

1. ไปที่ Dashboard → Project → Settings → Domains
2. ใส่ `korkornor` → Add
3. เสร็จ! จะได้ `https://korkornor.vercel.app`

---

## 🔗 เชื่อมต่อกับ Backend (Supabase)

### 1. สร้าง Supabase Project
1. ไปที่ [supabase.com](https://supabase.com)
2. สร้างโปรเจกต์ใหม่
3. ไปที่ SQL Editor
4. คัดลอกโค้ดจาก `supabase/schema.sql` แล้วรัน

### 2. ตั้งค่า Environment Variables

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
```

### 3. นำเข้าข้อมูลหน่วยเลือกตั้ง

```bash
export NEXT_PUBLIC_SUPABASE_URL="your-url"
export SUPABASE_SERVICE_ROLE_KEY="your-key"
node scripts/import-election-units.js data/election-units.json
```

---

## 📋 Checklist ก่อนใช้งานจริง

- [ ] Deploy บน Vercel (Mockup mode)
- [ ] สมัคร OpenRouter + ใส่ `OPENROUTER_API_KEY`
- [ ] สร้าง Supabase project + รัน schema
- [ ] ใส่ Supabase credentials
- [ ] นำเข้าข้อมูลหน่วยเลือกตั้ง (95,000+ หน่วย)
- [ ] ทดสอบ AI classification ด้วยรูปป้ายหน่วยเลือกตั้ง
- [ ] ตั้งค่า Custom Domain (ถ้ามี)

---

#8กุมภาเห็นชอบ 🇹🇭 #รัฐธรรมนูญฉบับประชาชน
